.env
output/
export.log
__pycache__/
*.pyc
.venv/

# --- Imported from former amacc/.gitignore during flatten ---
node_modules/
dist/
.env
*.log
.DS_Store
coverage/
.turbo/
prisma/migrations/
*.tsbuildinfo

# Security — never commit credentials or platform binaries
*.pem
*.key
*.p12
*.pfx
cloudflared*
*.exe
aws-key*

build/
*.js.map
.env.*
*.cert
*.crt
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
Thumbs.db
Desktop.ini
npm-debug.log*
.nyc_output/
*.py
*.py.bak
__pycache__/
*.tmp
*.zip
*.tar.gz

# Generated dev/test artifacts (R0 stabilization cleanup)
.vite/
playwright-report/
test-results/

# Backlog package is a tracked reference document, not a generated archive
!docs/accounting-modernization/AutoMate2_Accounting_Backlog_Package_v1.1.zip

# Terraform state, plan output, and provider module cache — these contain
# real resource IDs/secrets (state) or are large regenerable caches (module
# cache) and must never be committed.
**/.terraform/
*.tfstate
*.tfstate.*
tfplan
tfplan-*
tfplan[0-9]*
*.tfvars
!*.tfvars.example
node_modules
