# S023 — ACCOUNTING RULE MATRIX

**STATUS: `ACCOUNT_MAPPING_VALUES_PENDING` (D-S023-12 only — account-selection rules below are approved and final).** Every Debit/Credit account-selection **value** below is deliberately blank. D-S023-12 is `APPROVED_IN_PRINCIPLE` — the framework (event scope, trigger, amount source, posting-date rule, journal-source rule, schedule-effect, reference requirements, closed-period policy, reversal policy) is approved and, as of 2026-08-01, **final** as recorded in `S023_DECISION_REGISTER.md`; the actual account numbers are Accounting-owned, authored under identity-based SoD (author ≠ activator), tenant-configurable, versioned, and fully audited. **Engineering must not fill any blank DR/CR cell, and must never hardcode a tenant GL account number.** This file becomes v1 only through the governed authorship + activation process described at the bottom.

"Approved by" / "Approval date" in every row below cover the **row's framework/policy only** — not the DR/CR account values, which remain unapproved and pending per-tenant authorship.

**Both program dependencies that previously gated the "Schedule create/relieve effect" and "Required references" columns are now RESOLVED (2026-08-01)** — see `S023_DECISION_REGISTER.md` → "Two program dependencies — RESOLVED": (1) UQ-18 reference/schedule-key semantics, (2) the relieving-policy decision (JOURNAL_ENTRY_POSTED → schedule-service path). Every row below reflects the resolved semantics; no row is gated on either dependency any longer.

**AP tax-posting clarification (approved 2026-08-01):** `ap.invoice.accepted` is the single authoritative event that posts a vendor invoice's liability journal, including any tax lines. `ap.invoice.posted-request` is normalized to the same canonical event identity and idempotency key as `ap.invoice.accepted` — it is an internal posting-request / compatibility alias, not a second journal-producing event. See the dedicated row below for its normalization behavior; it no longer has an independent DR/CR mapping row of its own.

| Event type | Business transaction | Trigger | Debit account-selection rule | Credit account-selection rule | Amount source | Posting date | Journal source | Schedule create/relieve effect | Required references | Closed-period behaviour | Reversal/void behaviour | Approved by | Approval date |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ap.invoice.accepted` (single authoritative AP event — see clarification above) | Vendor invoice liability recognition, including any recoverable-tax or tax-expense lines | Invoice accepted / approved (S039 flow) | *(value pending)* — the account selected on each approved invoice distribution line: expense, inventory, asset, prepaid, or recoverable-tax account, as determined by the invoice distribution and approved tax mapping (D-18); dimensioned by store/dept | *(value pending)* — AP control account configured for the legal entity / vendor payable schedule | `envelope.accountingAmounts[]` — approved invoice distribution amount, including separately mapped tax lines (line-level, kind=NET/TAX per D-18) | `envelope.businessDate` = approved invoice business/document date | S212 registry — dedicated code per module family; exact code pending Engineering Step-0 confirmation of existing bootstrap rows | CREATE ap-open-item, via the resolved JOURNAL_ENTRY_POSTED → schedule-service path (no independent schedule-posting mechanism in the engine) | `scheduleNumber`=`account.scheduleCode`, `controlNumber`=vendor ID/approved control identifier, `applyNumber`=invoice/document number being created, `referenceNumber`=source document ID, `itemNumber`=`referenceNumber` (falls back to `journalEntryId` only if genuinely unavailable) — resolved UQ-18 model | Reject to DLQ (S021 recovery case) — never silent redating (D-15, approved) | Correction only via S218 reversal + corrected replay (D-27, approved); tax information arriving only after posting uses an explicitly governed tax-adjustment/correction workflow, never a silent re-post | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `ap.invoice.posted-request` (normalized alias — not an independent posting) | N/A — internal posting-request / compatibility alias for the same invoice | Any duplicate or secondary trigger referencing an invoice already posted (or being posted) under `ap.invoice.accepted` | N/A — never posts its own tax debit; see `ap.invoice.accepted` row | N/A — never posts its own AP-control credit; see `ap.invoice.accepted` row | N/A | N/A | N/A | N/A — must never create or relieve an open item independently | N/A | N/A | If the first-arriving event under the shared canonical identity is `ap.invoice.accepted`, then `ap.invoice.posted-request` for the same invoice returns the original posting result (existing D-24 idempotency mechanism, keyed on the shared canonical identity/idempotency key) — zero partial posting, zero duplicate journal, zero duplicate AP open item. Missing required tax lines/mappings on the authoritative event reject deterministically to S021, per D-20. | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `ap.payment.posted` | Manual vendor payment | Manual payment posted against an APPROVED invoice (S043A flow) | *(value pending)* — AP control account associated with the invoice/open item being relieved | *(value pending)* — bank/cash account configured for the selected payment account and legal entity | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | RELIEVE ap-open-item, via the resolved JOURNAL_ENTRY_POSTED → schedule-service path; **partial payment relieves only the applied amount and retains the remaining open balance** | Resolved UQ-18 model (as above); `applyNumber` matches the existing `itemNumber` of the invoice being relieved | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.receipt.applied` | Cash receipt application | Drawer cash receipt issued (S052 flow) | *(value pending)* — undeposited-funds/cash-clearing account configured for the cashier drawer, payment method, and legal entity | *(value pending)* — AR control account for amounts applied to customer invoices | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | RELIEVE ar-open-item by the applied amount, via the resolved JOURNAL_ENTRY_POSTED → schedule-service path | Resolved UQ-18 model (as above); `applyNumber` matches the existing `itemNumber` of the invoice being relieved | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.receipt.applied` — **unapplied amount (new row, approved 2026-08-01)** | Unapplied cash / customer credit | Same drawer receipt event, where all or part of the amount is not applied to a specific customer invoice at receipt time | *(value pending)* — undeposited-funds/cash-clearing account (same as the applied-amount row) | *(value pending)* — configured unapplied-cash / customer-credit account | `envelope.accountingAmounts[]` — the unapplied portion of the receipt amount | `envelope.businessDate` | S212 registry — code pending Step-0 | Retains the credit as an **open customer credit** (does not relieve any existing ar-open-item); no ap/ar open item is created for the unapplied portion beyond the customer-credit record itself | Resolved UQ-18 model (as above), scoped to the customer/legal entity rather than a specific invoice | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.receipt.applied` (clearing leg) | Cash-receipt clearing (deposit-to-cash precursor) | Same drawer receipt, clearing-leg posting | *(value pending)* — receipt account | *(value pending)* — cashier clearing account | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | — (no schedule effect) | — | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.deposit.posted` — **deposit-to-cash** | Bank deposit posting | Deposit posted against the approved deposit batch/bank account (S053 flow) | *(value pending)* — bank cash account selected by the approved deposit batch/bank account | *(value pending)* — undeposited-funds/cash-clearing account relieved by the deposit | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | **No AP or AR open item is created.** The deposit retains full linkage to its source receipts and deposit batch (no schedule-service open-item interaction) | Source-receipt and deposit-batch linkage (not a `schedule_key` reference — this event does not create/relieve an open item) | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `ar.invoice.posted-request` — **customer invoice/charge** | Customer invoice/charge posting | **DEFINED_NOT_YET_EXERCISED** — no producer exists; S048 has zero commits/branches anywhere in this repository | *(value pending)* — AR control account configured for the customer/legal entity | *(value pending)* — revenue, fee, and output-tax accounts selected from the approved invoice distribution and tax mappings (D-18) | `envelope.accountingAmounts[]` (kind=NET, tax per D-18) | `envelope.businessDate` | S212 registry — code pending Step-0 | CREATE ar-open-item, via the resolved JOURNAL_ENTRY_POSTED → schedule-service path | Resolved UQ-18 model (as above) | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only, scope approved; no producer to exercise it against yet) | 2026-08-01 |
| Rounding difference (D-19, all event types) | Sub-cent rounding remainder | Any posting where the absolute residual is $0.01 or less | *(value pending)* — configured legal-entity rounding-difference account | *(value pending)* — configured legal-entity rounding-difference account | Computed residual; **residual above $0.01 rejects the event to the S021 DLQ rather than force-balancing the journal** (D-19) | Same as triggering event | Same as triggering event | — | — | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |

## Authorship process

Accounting authors DR/CR values per tenant baseline (+ legal-entity overrides) → draft version → golden expected journal recorded per row (feeds AC-1 in `S023_ACCEPTANCE_CRITERIA.md`) → activation by a separately authorized user (identity-based SoD, D-28 — the author of the draft cannot activate it) → version pinned on every posting thereafter.

Banking rows: none (excluded, D-05). No suspense-account row exists, by decision (D-20 — missing/invalid account rejects, it does not fall back to a suspense account).

Any additional approved event type follows this same row shape and the same authorship process — no new columns, no invented values.
