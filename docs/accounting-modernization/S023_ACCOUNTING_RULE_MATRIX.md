# S023 — ACCOUNTING RULE MATRIX

**STATUS: `ACCOUNT_MAPPING_VALUES_PENDING`.** Every Debit/Credit account-selection **value** below is deliberately blank. D-S023-12 is `APPROVED_IN_PRINCIPLE` — the framework (event scope, trigger, amount source, posting-date rule, journal-source rule, schedule-effect slot, reference requirements, closed-period policy, reversal policy) is approved as recorded in `S023_DECISION_REGISTER.md`; the actual account numbers/selection values are Accounting-owned, authored under identity-based SoD (author ≠ activator), tenant-configurable, versioned, and fully audited. **Engineering must not fill any blank DR/CR cell.** This file becomes v1 only through the governed authorship + activation process described at the bottom.

"Approved by" / "Approval date" in every row below cover the **row's framework/policy only** — not the DR/CR account values, which remain unapproved and pending.

Two program dependencies are still outstanding and gate the "Schedule create/relieve effect" and "Required references" columns for every row that has one (see `S023_DECISION_REGISTER.md` → "Two surviving program dependencies"): **(1)** UQ-18 reference/schedule-key semantics, **(2)** the relieving-policy decision for schedule effects. Do not treat any row's schedule-effect or reference column as implementation-ready until both are supplied.

| Event type | Business transaction | Trigger | Debit account-selection rule | Credit account-selection rule | Amount source | Posting date | Journal source | Schedule create/relieve effect | Required references | Closed-period behaviour | Reversal/void behaviour | Approved by | Approval date |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `ap.invoice.accepted` | Vendor invoice liability recognition | Invoice accepted / approved (S039 flow) | *(pending)* — expense/asset per line, dimensioned by store/dept | *(pending)* — AP liability account | `envelope.accountingAmounts[]` (line-level, kind=NET, tax per D-18) | `envelope.businessDate` (per-event-type date table pending SME authorship alongside matrix) | S212 registry — dedicated code per module family; exact code pending Engineering Step-0 confirmation of existing bootstrap rows | CREATE ap-open-item — key mapping pending the two surviving program dependencies above | Per UQ-18 `schedule_key` model (`scheduleNumber`/`controlNumber`/`applyNumber`/`referenceNumber`) — exact per-row mapping pending relieving-policy decision | Reject to DLQ (S021 recovery case) — never silent redating (D-15, approved) | Correction only via S218 reversal + corrected replay (D-27, approved) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `ap.invoice.posted-request` (tax line) | Tax-line mapping on vendor invoice | Invoice posting request carrying received tax lines | *(pending)* — tax line per D-18, store-dimensioned | *(pending)* | `envelope.accountingAmounts[]` (kind=TAX, received only — zero calculation, D-18) | `envelope.businessDate` | S212 registry — code pending Step-0 | — (no schedule effect) | — | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `ap.payment.posted` | Manual vendor payment | Manual payment posted against an APPROVED invoice (S043A flow) | *(pending)* — AP liability relief | *(pending)* — bank/cash account selected at payment | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | RELIEVE ap-open-item — policy pending relieving-policy decision (see above) | Per UQ-18 `schedule_key` model — exact mapping pending relieving-policy decision | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.receipt.applied` | Cash receipt application | Drawer cash receipt issued (S052 flow) | *(pending)* — cash/clearing per receipt type, store/dept-dimensioned | *(pending)* — AR relief or income per receipt type | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | RELIEVE ar-open-item — key mapping pending the two surviving program dependencies above | Per UQ-18 `schedule_key` model — exact per-row mapping pending relieving-policy decision | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.receipt.applied` (clearing leg) | Cash-receipt clearing (deposit-to-cash precursor) | Same drawer receipt, clearing-leg posting | *(pending)* — receipt account | *(pending)* — cashier clearing account | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | — (no schedule effect) | — | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `cash.deposit.posted` — **deposit-to-cash** | Bank deposit posting | Deposit posted against accumulated clearing balance (S053 flow) | *(pending)* — clearing account | *(pending)* — bank/cash GL account | `envelope.accountingAmounts[]` (kind=GROSS) | `envelope.businessDate` | S212 registry — code pending Step-0 | SETTLE clearing item — policy pending relieving-policy decision | — | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |
| `ar.invoice.posted-request` — **customer invoice/charge** | Customer invoice/charge posting | **DEFINED_NOT_YET_EXERCISED** — no producer exists; S048 has zero commits/branches anywhere in this repository | *(pending)* — AR control per line | *(pending)* — revenue per line | `envelope.accountingAmounts[]` (kind=NET, tax per D-18) | `envelope.businessDate` | S212 registry — code pending Step-0 | CREATE ar-open-item — key mapping pending the two surviving program dependencies above | Per UQ-18 `schedule_key` model — exact per-row mapping pending relieving-policy decision | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only, scope approved; no producer to exercise it against yet) | 2026-08-01 |
| Rounding difference (D-19, all event types) | Sub-cent rounding remainder | Any posting where independent per-line rounding leaves a remainder | *(pending)* — entity rounding-difference account | *(pending)* — entity rounding-difference account | Computed remainder (half-up line rounding; >|$0.01| imbalance rejects, D-19) | Same as triggering event | Same as triggering event | — | — | Reject to DLQ — never silent redating (D-15) | Correction only via S218 reversal + corrected replay (D-27) | Shivashankar Angadi (framework/policy only) | 2026-08-01 |

## Authorship process

Accounting authors DR/CR values per tenant baseline (+ legal-entity overrides) → draft version → golden expected journal recorded per row (feeds AC-1 in `S023_ACCEPTANCE_CRITERIA.md`) → activation by a separately authorized user (identity-based SoD, D-28 — the author of the draft cannot activate it) → version pinned on every posting thereafter.

Banking rows: none (excluded, D-05). No suspense-account row exists, by decision (D-20 — missing/invalid account rejects, it does not fall back to a suspense account).

Any additional approved event type follows this same row shape and the same authorship process — no new columns, no invented values.
